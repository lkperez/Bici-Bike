<div class="page-section flexbox vertical stretch">
  <div class="section-inner flexbox vertical stretch">
    <div class="section-title">
      Rent a Bike
    </div>
    <div class="rides new">
      <%= form_for @ride do |f| %>
        <%= f.label :length %>:
        <%= f.number_field :length %><br/>

        <%= f.submit %>
      <% end %>
    </div>
	</div>
</div>
